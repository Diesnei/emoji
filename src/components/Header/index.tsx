import { ReactNode } from 'react';
import './styles.scss';

export default function Header(): ReactNode {
    return (
        <header>
            <h1>
                Choose your emoji!
            </h1>
        </header>
    )
}